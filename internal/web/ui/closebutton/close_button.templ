// Package closebutton is vendored from workspace/heroui-go (components/close_button/close_button.templ).
package closebutton

import "mahrec/internal/web/ui/utils"

type Props struct {
	IsDisabled bool
	Attrs      templ.Attributes
}

templ CloseButton(p Props, tw ...string) {
	@closeButtonRoot(p, tw...) {
		@closeIcon()
	}
}

templ closeButtonRoot(p Props, tw ...string) {
	<button
		type="button"
		class={ utils.Cn("close-button", "close-button--default", tw) }
		if p.Attrs["data-slot"] == nil {
			data-slot="close-button"
		}
		if p.Attrs["aria-label"] == nil {
			aria-label="Close"
		}
		disabled?={ p.IsDisabled }
		{ p.Attrs... }
	>
		{ children... }
	</button>
}

templ closeIcon() {
	<svg
		data-slot="close-button-icon"
		xmlns="http://www.w3.org/2000/svg"
		viewBox="0 0 16 16"
		width="16"
		height="16"
		fill="none"
		aria-hidden="true"
	>
		<path
			fill-rule="evenodd"
			clip-rule="evenodd"
			fill="currentColor"
			d="M3.47 3.47a.75.75 0 0 1 1.06 0L8 6.94l3.47-3.47a.75.75 0 1 1 1.06 1.06L9.06 8l3.47 3.47a.75.75 0 1 1-1.06 1.06L8 9.06l-3.47 3.47a.75.75 0 0 1-1.06-1.06L6.94 8 3.47 4.53a.75.75 0 0 1 0-1.06Z"
		></path>
	</svg>
}
